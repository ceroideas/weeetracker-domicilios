import { ListadoRaeePipe } from './listado-raee.pipe';

describe('ListadoRaeePipe', () => {
  it('create an instance', () => {
    const pipe = new ListadoRaeePipe();
    expect(pipe).toBeTruthy();
  });
});
